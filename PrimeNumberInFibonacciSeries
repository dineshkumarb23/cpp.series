#include <iostream>
#include<math.h>
using namespace std;

int main(int argc, char** argv)
{ int n,x=-1,y=1,i=x+y,d=0,j;
  cin>>n;
  while(n>=i)
  { 
  if(i>1){
    for(j=2;j<=sqrt(i);j++)
    if(i%j==0) {d=1;break;}
    if(d==0) cout<<i<<" ";
    d=0;
  }
    x=y;y=i;
    i=x+y;
   }
 
}
